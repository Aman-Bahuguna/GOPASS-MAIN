/**
 * ==========================================
 * MOCK DATA - Central Export
 * ==========================================
 * 
 * All mock/dummy data lives in this folder.
 * When the backend is ready, the API layer (src/api/)
 * will replace these imports — no changes needed here.
 * 
 * Files:
 *   users.js          – mockAdmins, mockOrganizers, mockUsers
 *   events.js         – mockEvents
 *   registrations.js  – mockRegistrations
 */

// --- Raw data ---
export { mockAdmins, mockOrganizers, mockUsers } from './users';
export { mockEvents } from './events';
export { mockRegistrations } from './registrations';
